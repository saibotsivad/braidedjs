export const parseUrl = requestUrl => {
	const url = new URL(requestUrl)
	url.query = (url.search && url.search.replace(/^\?/, '') || '')
		.split('&')
		.reduce((map, keyValue) => {
			const [ key, value ] = keyValue.split('=')
			map[key] = value
			return map
		}, {})
	return url
}

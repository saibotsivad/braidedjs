import { handleRequest } from './handle-request.js'
import { test } from 'zora'

test('making a request', async t => {
	const result = await handleRequest({
		url: 'https://example.com/123/games',
		method: 'GET'
	})
	t.equal(result, 'foo')
})

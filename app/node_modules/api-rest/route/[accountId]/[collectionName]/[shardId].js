import { etagIfNoneMatch } from 'shared/etag-if-none-match.js'

export const routes = {
	path: '/:accountId/:collectionName/:shardId',
	methods: {
		GET: async (request, { accountId, collectionName, shardId, originalUrl, lastUpdated }, service) => {
			// TODO
		}
	}
}

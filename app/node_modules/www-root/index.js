import { handleRequest } from './handle-request.js'

const service = {
	// add KV stuff for A/B testing
}

addEventListener('fetch', event => {
	event.respondWith(handleRequest(event.request, service))
})

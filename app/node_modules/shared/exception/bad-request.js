export class BadRequestException extends Error {
	constructor(message) {
		super(message)
		this.code = 'BadRequestException'
		this.title = 'Bad Request'
		this.status = '400'
		this.detail = message || 'Request contained invalid parameters.'

		if (meta || typeof message === 'object') {
			this.meta = meta
		}
	}
}

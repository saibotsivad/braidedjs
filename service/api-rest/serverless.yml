service:
  name: braided-api-rest

provider:
  name: cloudflare
  config:
    accountId: ${env:CLOUDFLARE_ACCOUNT_ID}
    zoneId: ${env:CLOUDFLARE_ZONE_ID}

plugins:
  - serverless-cloudflare-workers

functions:
  api:
    name: braided-api-rest
    script: index
    resources:
      kv:
        - variable: CREDENTIALS
          namespace: BRAIDED_REST_API_CREDENTIALS
        - variable: ACCOUNTS
          namespace: BRAIDED_REST_API_SCHOOLS
    events:
      - http:
          url: api.braided.com/*
          method: GET
